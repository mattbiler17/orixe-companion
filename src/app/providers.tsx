import React from 'react'

type Props = { children: React.ReactNode }

export default function Providers({ children }: Props) {
  // Placeholder for future providers (Zustand, context providers, theming)
  return <>{children}</>
}
